/*
 *  Dieses Datei hat die Mathematische Funktionen, die bein ein andere Datei verwändet wird
 *
 *  @Rafael Xavier
 **/

public class RundeFormen{
  /*
   * Diese Methode berechnet die Fläche eines Kreises mit ein r parameter (A = r² * p)
   * @parameter r geht für den Radius, der in der Rechnung verwändet wird.
   * @return die Kreisfläche
   *
   **/
    static double kreisFlaeche(double r){
        return (Math.pow(r,2) * Math.PI);
      }

  /*
   * Diese Methode berechnet die Oberfläche einer Kugel mit ein r parameter (A = 4 * r² * p)
   * @parameter r geht für den Radius, der in der Rechnung verwändet wird.
   * @return die Oberfläche einer Kugel 
   *
   **/    
    static double kugelOberflaesche (double r){
        return (4 * Math.pow(r,2) * Math.PI);
      }

  /*
   * Diese Methode berechnet das Volumen einer Kugel mit ein r parameter (V = 4/3 * r³ * p)
   * @parameter r geht für den Radius, der in der Rechnung verwändet wird.
   * @return das Volumen einer Kugel 
   *
   **/      
    static double kugelVolumen (double r){
        return (4/3 * Math.pow(r,3) * Math.PI);
    }
}
